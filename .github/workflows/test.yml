name: Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

concurrency:
  group: test-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js 20.x
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci

      - name: Audit steering hygiene
        run: npm run audit:steering

      - name: Audit clarification-first baseline
        run: |
          node scripts/clarification-first-audit.js \
            --fail-on-violation \
            --out .sce/reports/clarification-first-audit-ci.json \
            --json

      - name: Audit release docs
        run: |
          node scripts/release-doc-version-audit.js \
            --fail-on-error \
            --out .sce/reports/release-doc-version-audit-ci.json \
            --json
      
      - name: Run tests
        run: npm test -- --runInBand

      - name: Run interactive loop smoke
        run: npm run test:interactive-loop-smoke

      - name: Run interactive flow smoke
        run: npm run test:interactive-flow-smoke

      - name: Run Moqui rebuild gate
        run: npm run gate:moqui-rebuild

      - name: Build Moqui baseline (CI)
        run: |
          node scripts/moqui-template-baseline-report.js \
            --out .sce/reports/moqui-template-baseline-ci.json \
            --markdown-out .sce/reports/moqui-template-baseline-ci.md \
            --json

      - name: Evaluate matrix regression gate (CI)
        env:
          MATRIX_REGRESSION_GATE_ENFORCE: ${{ vars.SCE_MATRIX_REGRESSION_GATE_ENFORCE }}
          MATRIX_REGRESSION_GATE_MAX: ${{ vars.SCE_MATRIX_REGRESSION_GATE_MAX }}
        run: |
          set -euo pipefail
          ENFORCE_RAW="${MATRIX_REGRESSION_GATE_ENFORCE:-false}"
          ENFORCE_NORMALIZED="$(printf '%s' "${ENFORCE_RAW}" | tr '[:upper:]' '[:lower:]')"
          ENFORCE_FLAG=""
          if [[ "${ENFORCE_NORMALIZED}" == "1" || "${ENFORCE_NORMALIZED}" == "true" || "${ENFORCE_NORMALIZED}" == "yes" || "${ENFORCE_NORMALIZED}" == "y" || "${ENFORCE_NORMALIZED}" == "on" ]]; then
            ENFORCE_FLAG="--enforce"
          fi
          MAX_REGRESSIONS="${MATRIX_REGRESSION_GATE_MAX:-0}"
          node scripts/matrix-regression-gate.js \
            --baseline .sce/reports/moqui-template-baseline-ci.json \
            --max-regressions "${MAX_REGRESSIONS}" \
            --out .sce/reports/release-evidence/matrix-regression-gate-ci.json \
            ${ENFORCE_FLAG} \
            --json

      - name: Run interactive governance gate
        run: node scripts/interactive-governance-report.js --period weekly --fail-on-alert --json

      - name: Build Moqui release summary (CI advisory)
        run: |
          node scripts/moqui-release-summary.js \
            --baseline .sce/reports/moqui-template-baseline-ci.json \
            --interactive-governance .sce/reports/interactive-governance-report.json \
            --out .sce/reports/release-evidence/moqui-release-summary-ci.json \
            --markdown-out .sce/reports/release-evidence/moqui-release-summary-ci.md \
            --json

      - name: Check coverage
        run: npm run coverage -- --runInBand
      
      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella

      - name: Upload interactive governance and matrix artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: interactive-governance-matrix-${{ github.run_id }}
          if-no-files-found: warn
          path: |
            .sce/reports/interactive-governance-report.json
            .sce/reports/interactive-governance-report.md
            .sce/reports/clarification-first-audit-ci.json
            .sce/reports/release-doc-version-audit-ci.json
            .sce/reports/interactive-matrix-signals.jsonl
            .sce/reports/interactive-flow-smoke/interactive-flow-smoke.summary.json
            .sce/reports/moqui-template-baseline-ci.json
            .sce/reports/moqui-template-baseline-ci.md
            .sce/reports/release-evidence/moqui-release-summary-ci.json
            .sce/reports/release-evidence/moqui-release-summary-ci.md
            .sce/reports/release-evidence/matrix-regression-gate-ci.json
