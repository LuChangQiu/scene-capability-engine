# Tasks

- [ ] Define the first engine-owned delivery projection envelope and object schema set.
- [ ] Specify canonical identity rules for artifact, handoff, release, and acceptance objects.
- [ ] Define explicit scope back-link rules to `scene/spec/task/event` truth.
- [ ] Document the semantic boundary between execution streams, evidence links, and delivery-governance projection records.
- [ ] Document the engine-owned versus adapter-owned responsibility split for IDE and CLI consumers.
- [ ] Decide the phase-1 transport surface for exposing the delivery projection contract.
- [ ] Identify which delivery object ids can be canonical immediately and which must ship as provisional.
- [ ] Preserve compatibility with existing scene execution flows while adding delivery projection support.
