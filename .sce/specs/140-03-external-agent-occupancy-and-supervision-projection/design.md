# Design Document

## Scope

`140-03` owns:

- occupancy projection schema
- supervision projection extension for external runtimes
- explicit missing-data semantics

It reuses existing SCE supervision direction instead of creating a new plane.
