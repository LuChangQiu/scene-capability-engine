apiVersion: sce.scene/v0.2
kind: scene
metadata:
  obj_id: scene.erp.40-00-scene-scaffold-command
  obj_version: 0.2.0
  title: Scene Scaffold Command
spec:
  domain: erp
  intent:
    goal: Query order detail and return normalized response
  model_scope:
    read:
      - moqui.OrderHeader.orderId
      - moqui.OrderHeader.statusId
    write: []
  capability_contract:
    bindings:
      - type: query
        ref: spec.erp.order-query-service
        timeout_ms: 2000
        retry: 0
  governance_contract:
    risk_level: low
    approval:
      required: false
    idempotency:
      required: true
      key: orderId
