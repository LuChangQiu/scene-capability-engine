# 任务文档

## 任务列表

- [ ] 定义本地 Agent 过程检查点的标准类型与字段表
- [ ] 补充 stdout/jsonl 等不同传输承载方式的非绑定示例
- [ ] 明确 `blocker`、`done`、`handoff` 的治理语义
- [ ] 明确与 `scene > spec > task > event` 的映射关系
- [ ] 明确与 `studio events` / task feedback / timeline 的非平行映射边界
- [ ] 为 IDE / CLI / 嵌入式宿主提供最小接入示例
