apiVersion: sce.scene/v0.2
kind: scene
metadata:
  obj_id: scene.moqui-form-interaction-core
  obj_version: 0.1.0
  title: Moqui Form Interaction Core
spec:
  domain: moqui
  intent:
    goal: Recover Moqui form schema and interaction fields.
  model_scope:
    read:
      - spec.moqui.form.discover
      - spec.moqui.form.field.bind
    write: []
  capability_contract:
    bindings:
      - type: operation
        ref: spec.moqui.form.discover
        intent: Discover baseline metadata and topology.
      - type: operation
        ref: spec.moqui.form.field.bind
        depends_on: spec.moqui.form.discover
        intent: Apply normalization and mapping for rebuild.
  governance_contract:
    risk_level: medium
    approval:
      required: false
    idempotency:
      required: true
      key: moqui-form-interaction-coreFingerprint
