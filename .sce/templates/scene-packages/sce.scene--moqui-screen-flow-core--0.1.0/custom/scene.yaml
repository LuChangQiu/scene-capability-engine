apiVersion: sce.scene/v0.2
kind: scene
metadata:
  obj_id: scene.moqui-screen-flow-core
  obj_version: 0.1.0
  title: Moqui Screen Flow Core
spec:
  domain: moqui
  intent:
    goal: Recover Moqui screen flow and screen/service graph.
  model_scope:
    read:
      - spec.moqui.screen.discover
      - spec.moqui.screen.service.bind
    write: []
  capability_contract:
    bindings:
      - type: operation
        ref: spec.moqui.screen.discover
        intent: Discover baseline metadata and topology.
      - type: operation
        ref: spec.moqui.screen.service.bind
        depends_on: spec.moqui.screen.discover
        intent: Apply normalization and mapping for rebuild.
  governance_contract:
    risk_level: medium
    approval:
      required: false
    idempotency:
      required: true
      key: moqui-screen-flow-coreFingerprint
